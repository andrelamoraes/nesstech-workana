node_modules/
.env
*.log

# Credenciais criptografadas, sessão do navegador, diagnósticos e vagas
# capturadas. NUNCA versionar — data/db.json guarda o cofre e, assim que
# Configurações é preenchido, a senha do Workana e a chave da IA (encriptadas,
# mas ainda assim não pertencem a um repositório, ainda mais um público).
# data/workana-session.json guarda cookies de sessão ativa: quem tiver esse
# arquivo entra na conta do Workana sem senha nenhuma.
data/

# Configuração local do Claude Code (específica desta máquina)
.claude/
